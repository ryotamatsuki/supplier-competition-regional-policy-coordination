# Stage 7 — Independent Hostile Manuscript / Proof Audit

## Verdict

`REPAIR — THEORY INTACT`

The headline theorems survive. Submission is not approved until the following repairs are implemented:

1. Correct the full mixed-strategy Nash correspondence at `A=A_N^N`; the full boundary set is `{(p_1,p_2) in [0,1]^2 : p_1=1 or p_2=1}`.
2. Replace the informal reserve/trade-failure limit argument with a dominated-convergence proof using `0 <= r_N^R <= C_{2:N}-C_{1:N}`.
3. Clarify `rho`: the uncaptured supplier surplus is outside the D government's objective but remains within the coordinated benchmark's constituency in the baseline.
4. Clarify the institutional mapping: U is the lead/buyer side; D hosts/develops the supplier-side base.
5. Add an Elsevier-compliant generative-AI declaration and a data-availability statement.
6. Add a reproducible Uniform-regime figure.
7. Expand direct prior-art treatment where relevant, especially local procurement/regional requirements and the 2026 OECD procurement-industrial-policy report.

No reason was found to reopen Stage 0–6 theory.